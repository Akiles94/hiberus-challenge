import styled from 'styled-components';

const StyledWrapper = styled.div`
  display: flex;
`;

export default function Users() {
  return (
    <StyledWrapper>
      <h1>Usuarios</h1>
    </StyledWrapper>
  );
}
