import styled from 'styled-components';

const StyledWrapper = styled.div`
  display: flex;
`;

const Login = () => {
  return (
    <StyledWrapper>
      <h1>Login</h1>
    </StyledWrapper>
  );
};

export default Login;
